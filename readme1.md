# DjangoForEveryone

<!-- by Liang Zegui -->
A Django tutorial project repository for beginners, containing basic application development examples and phased tutorial code to help users systematically learn the Django framework.

✨ Project Features  
🏗️ Built with Django 4.0+  
📚 Branch-managed tutorial progress (e.g., post-tutorial branch contains complete code)  
🧩 Modular design with core example app "baseapp"  
🖥️ MySQL database configuration examples  
📊 Integrated template system and static resource management  
📝 Detailed development documentation and sample code  
🚀 Suitable for beginners and intermediate developers  

🚀 Quick Start  
Clone repository & switch branch  
```bash
git clone https://github.com/yourname/DjangoForEveryone.git
cd DjangoForEveryone
git checkout post-tutorial  # Switch to completed branch

Environment Setup
Ensure Python 3.10+ is installed and create virtual environment:

bash
pip install -r requirements.txt
Start Server

bash
python manage.py runserver
Visit http://127.0.0.1:8000/ to view the project.

#### Project Structure
The project structure is as follows:
DJANGOFOREVERYONE-1/
├── baseapp/
│   ├──.idea/               # PyCharm or other JetBrains IDE configuration files
│   ├── baseapp/             # Django project configuration directory (contains settings.py, urls.py, etc.)
│   ├── main/                # Django application directory (custom app)
│   ├── static/              # Static files (CSS, JS, images, etc.)
│   ├── templates/           # HTML template files
│   ├── db.sqlite3           # SQLite database file
│   ├── manage.py            # Django command-line tool
│   └── README.md            # Project documentation
└── venv/
├── README.md            # Virtual environment documentation
├── readme.zh.md         # Chinese documentation
└── readme1.md           # Other documentation

📸 Feature Examples & Screenshots

Data Model Admin Interface
images\后台管理界面.png
Description: Manage baseapp data models through Django Admin

Template Rendering
images\系统渲染的动态页面.png
images\系统渲染的动态页面1.png
Description: Dynamic pages rendered using Django's template system

📚 English Glossary
images\英语术语表.png

┌───────────────┬──────────────────┬────────────────────────────────────────────┐
│  English Term │  Chinese Term    │               Description                  │
├───────────────┼──────────────────┼────────────────────────────────────────────┤
│ Migration     │ 数据迁移         │ Django's database version control system  │
│ ORM           │ 对象关系映射     │ Database operations via Python classes     │
│ Middleware    │ 中间件           │ Handles request/response processing        │
│ Template      │ 模板             │ HTML page generation system               │
│ Static Files  │ 静态文件         │ CSS/JS/image resources                     │
│ URL           │ 统一资源定位符   │ Web resource identifier                    │
│ View          │ 视图             │ Handles HTTP requests & returns responses  │
│ Model         │ 模型             │ Data structure definition class            │
└───────────────┴──────────────────┴────────────────────────────────────────────┘

<!-- by Liang Zegui -->


<!-- by liaodequan -->

##Annotated Project Structure and Corresponding Functions

Root Directory Files and Folders
DjangoForEveryone/
├── README.md # Project documentation, typically containing the project introduction, usage instructions, architecture, etc. Here, it introduces that this project is for JimShapedCoding's Django tutorial, as well as the basic structure of the project and how to access other content.
├── baseapp/ # A Django application used in the tutorial to explain and demonstrate related topics.
├── venv/ # Python virtual environment directory, used to isolate project dependencies and avoid conflicts between different projects.

baseapp Application Directory
baseapp/
├── .idea/ # Configuration folder automatically generated by IDEs like IntelliJ IDEA or PyCharm, containing project-specific IDE settings.
├── baseapp/ # This is the Django project's configuration directory, containing the project's settings files.
│ ├── settings.py # The project's main settings file, including database configuration, app list, static file paths, etc.
│ ├── urls.py # The project's URL routing configuration file, defining the URL mapping relationships.
│ ├── wsgi.py # Configuration file for the WSGI server used to deploy the project.
├── main/ # This is a Django app directory, likely containing the app's views, models, templates, etc.
│ ├── models.py # Defines the app's data models, i.e., the database table structures.
│ ├── views.py # Handles user requests and returns responses via view functions or classes.
│ ├── urls.py # The app's URL routing configuration file, defining the URL mapping relationships within the app.
├── static/ # Stores the project's static files, such as CSS, JavaScript, images, etc. These files are directly referenced in the project and do not require server processing.
├── templates/ # Stores the project's HTML template files, used to generate dynamic web pages. Django combines data with templates via the template engine to produce the final HTML pages.
├── db.sqlite3 # The default SQLite database file used by Django, storing the project's data.
├── manage.py # Django project management script, used to execute various management commands, such as creating apps, running the development server, performing database migrations, etc.

venv Virtual Environment Directory
venv/
├── Lib/ # Directory for Python libraries installed in the virtual environment.
├── Scripts/ # Scripts directory for the virtual environment, containing scripts to activate and deactivate the virtual environment, as well as shortcuts to the Python interpreter.
├── pyvenv.cfg # Configuration file for the virtual environment, containing basic information and configuration options.

images Directory (for storing images)
images/

<!-- by liaodequan -->